# 🏗️ Open RTS 框架设计理念与扩展指南

---

## 一、核心设计理念

这个框架虽然代码量不少，但**设计意图非常清晰**——它遵循了游戏开发中经典的**组合优于继承**和**数据驱动**思想。总结下来有四大核心理念：

### 理念 1：组合（Composition）> 继承（Inheritance）

传统做法是用继承树来定义单位类型：
```
Unit → 可移动单位 → 地面单位 → 坦克
     → 建筑 → 生产建筑 → 指挥中心
```

但这个框架采用了**组件式设计（Entity-Component）**：

```
┌─────────────────────────────────┐
│            Tank 实体             │
│  ├── Movement（移动能力）        │
│  ├── HealthBar（血条显示）       │
│  ├── Selection（选中反馈）       │
│  ├── Highlight（悬停高亮）       │
│  └── Targetability（可被攻击）   │
└─────────────────────────────────┘
```

**单位不是通过"它是什么"来定义，而是通过"它有什么组件"来定义。** 坦克能移动因为有 Movement 组件，指挥中心能生产因为有 ProductionQueue 组件。

### 理念 2：行为即对象（Action as Object）

每个单位正在做的事情被封装为一个**独立的动作对象（Action）**，挂在单位节点下作为子节点。

```
Unit
├── Movement（特性）
├── HealthBar（特性）
├── [Action: Moving]（当前动作）  ← 动作会随时间变化
```

- 动作可以**随时切换**（移动中接到攻击命令 → 替换为攻击动作）
- 动作完成时**自动销毁**（`queue_free()`）
- 动作之间**互相独立**，新增一种动作不影响已有代码

### 理念 3：数据驱动（Data-Driven）

单位的血量、攻击力、造价、生产时间等**不在脚本里硬编码**，而是统一写在 `MatchConstants.gd` 的配置字典里：

```gdscript
# MatchConstants.gd 中的配置
const DEFAULT_PROPERTIES = {
    "res://source/match/units/Tank.tscn": {
        "hp": 10, "hp_max": 10,
        "attack_damage": 2,
        "attack_range": 5.0,
        "sight_range": 8.0,
    },
    "res://source/match/units/Worker.tscn": {
        "hp": 6, "hp_max": 6,
        "sight_range": 5.0,
        "resources_max": 2,
    },
}
```

单位脚本在 `_ready()` 时自动从常量表里加载自己的属性，**逻辑和数据完全分离**。

### 理念 4：信号驱动（Signal-Driven）

框架中大量的通信通过 Godot 信号实现，模块之间不直接调用对方的方法，而是"喊一声"让关心的人去处理：

```
MatchSignals.unit_died.emit(unit)
    └→ MatchEndHandler 收到 → 检查是否游戏结束
    └→ 敌方 AI 收到 → 调整策略
    └→ UI 收到 → 更新信息
```

这样就做到了**松耦合**——发送者不需要知道谁会响应。

---

## 二、框架的 5 层架构

从宏观上看，整个框架可以分为五个层次，从上到下依赖：

```
┌─────────────────────────────────────────┐
│  🖥️   UI 层 (main-menu, match/hud)      │
│      主菜单、小地图、资源条、单位菜单    │
├─────────────────────────────────────────┤
│  🎮  玩家层 (match/players)              │
│      人类玩家、AI 控制器                 │
├─────────────────────────────────────────┤
│  🚁  单位层 (match/units)                │
│      Unit + Traits(特性) + Actions(动作) │
├─────────────────────────────────────────┤
│  🌍  世界层 (match/)                     │
│      地形、导航、战争迷雾、摄像机        │
├─────────────────────────────────────────┤
│  📋  数据层 (Constants, data-model)      │
│      常量配置、游戏设置、存档            │
└─────────────────────────────────────────┘
```

### 各层职责

| 层级 | 负责什么 | 核心文件 |
|------|---------|---------|
| **数据层** | 配置数据、游戏设置、存档读写 | `Constants.gd`, `MatchConstants.gd`, `data-model/` |
| **世界层** | 地图、地形、寻路、战争迷雾、摄像机 | `Map.gd`, `Navigation.gd`, `FogOfWar.gd`, `IsometricCamera3D.gd` |
| **单位层** | 单位的属性、行为、战斗、采集、建造 | `Unit.gd`, `Structure.gd`, `actions/`, `traits/` |
| **玩家层** | 决策输入（人类鼠标操作 / AI逻辑） | `Player.gd`, `Human.gd`, `SimpleClairvoyantAI.gd` |
| **UI层** | 界面显示、交互菜单 | `main-menu/`, `hud/` |

每一层通过**信号**与上下层通信：

```
玩家层 ──(下达指令)──→ 单位层
单位层 ──(发出信号)──→ UI 层（更新显示）
世界层 ──(提供数据)──→ 单位层（导航路径、迷雾信息）
```

---

## 三、新增功能的通用设计步骤

**无论你想加什么新功能，都遵循这 5 步流程：**

### Step 1：确定属于哪一层

先想清楚这个功能属于哪个层次：

| 你要加的功能 | 属于哪层 |
|------------|---------|
| 新兵种（如火箭兵） | 单位层 → 新单位 + 新动作（可选） |
| 新建筑（如雷达站） | 单位层 → 新继承 Structure |
| AI新策略（如偷袭） | 玩家层 → 新 AI 控制器 |
| 新 UI（如科技树） | UI 层 → 新 HUD 组件 |
| 新地图 | 世界层 → 地图场景 |
| 新资源类型（如天然气） | 数据层 + 单位层 |

### Step 2：使用框架约定创建文件

- 每个功能是一个**场景（.tscn）+ 脚本（.gd）** 配对
- 放在对应的子目录下
- 遵循已有的命名规范（PascalCase）

### Step 3：注册数据到 Constants

- 如果新增的是单位，到 `MatchConstants.gd` 的 `DEFAULT_PROPERTIES` 注册属性
- 如果有生产成本，到 `PRODUCTION_COSTS` 和 `PRODUCTION_TIMES` 注册
- 如果是建筑，到 `CONSTRUCTION_COSTS` 和 `STRUCTURE_BLUEPRINTS` 注册

### Step 4：通过 Signal 连接新功能

- 使用现有信号（如 `unit_spawned`, `unit_died`）比新建信号更好
- 如果确实需要，在 `MatchSignals.gd` 中添加新信号
- 在 `_ready()` 中连接信号，在 `_exit_tree()` 中断开

### Step 5：装配测试

- 手动在场景里放一个测试，或参照 `tests/` 下的测试样例
- 在 `play.gd` 的设置界面里配置新单位/新地图

---

## 四、5 个实战案例

### 案例 1：新增一个"火箭兵"单位

火箭兵是地面可移动单位，能对空对地攻击。

**① 创建单位脚本**

```gdscript
# source/match/units/RocketSoldier.gd
extends "res://source/match/units/Unit.gd"

const WaitingForTargets = preload("res://source/match/units/actions/WaitingForTargets.gd")

func _ready():
    await super()
    action_changed.connect(_on_action_changed)
    action = WaitingForTargets.new()

func _on_action_changed(new_action):
    if new_action == null:
        action = WaitingForTargets.new()
```

**② 创建场景，组合特性**

在 Godot 编辑器中创建 `RocketSoldier.tscn`：
- 根节点：继承 `Unit.gd` 的 Area3D
- 添加子节点：`Movement`（地面，速度 3.0）+ `HealthBar` + `Selection` + `Highlight` + `Targetability`
- 添加 3D 模型到 Geometry 子节点（用 Kenney 的模型或自制）
- 配置 Movement 的参数：radius=0.7, speed=3.0

**③ 注册到 Constants**

```gdscript
# 在 MatchConstants.gd 的 DEFAULT_PROPERTIES 中添加
"res://source/match/units/RocketSoldier.tscn": {
    "sight_range": 7.0,
    "hp": 8,
    "hp_max": 8,
    "attack_damage": 2,
    "attack_interval": 1.5,
    "attack_range": 6.0,
    "attack_domains": [Navigation.Domain.TERRAIN, Navigation.Domain.AIR],
},

# 在 PRODUCTION_COSTS 中添加（如果可以从工厂生产）
"res://source/match/units/RocketSoldier.tscn": {
    "resource_a": 2,
    "resource_b": 1,
},

# 在 PRODUCTION_TIMES 中添加
"res://source/match/units/RocketSoldier.tscn": 4.0,
```

**④ 更新 VehicleFactory 的生产列表**

在 `VehicleFactory.gd` 中添加对 RocketSoldier 的引用。

**⑤ 创建 HUD 菜单**

参考 `WorkerMenu.gd` 创建 `RocketSoldierMenu.gd`，在 `unit-menus/` 下。

---

### 案例 2：新增一个"雷达站"建筑

雷达站能探测大范围敌人（但不能攻击）。

**① 创建脚本**

```gdscript
# source/match/units/RadarStation.gd
extends "res://source/match/units/Structure.gd"

# 雷达站的特点：超大视野
# 视野在 Constants 中设置，脚本几乎不需要额外代码
```

**② 创建场景**

`RadarStation.tscn` 组合：
- `MovementObstacle`（作为障碍物）
- `HealthBar` + `Selection` + `Highlight` + `Targetability`
- 3D 模型

**③ 注册数据**

```gdscript
# 在 DEFAULT_PROPERTIES 中
"res://source/match/units/RadarStation.tscn": {
    "sight_range": 20.0,  # 超远视野
    "hp": 12,
    "hp_max": 12,
},

# 在 CONSTRUCTION_COSTS 中
"res://source/match/units/RadarStation.tscn": {
    "resource_a": 3,
    "resource_b": 5,
},

# 可选：在 STRUCTURE_BLUEPRINTS 中注册蓝图
"res://source/match/units/RadarStation.tscn":
    "res://source/match/units/structure-geometries/RadarStation.tscn",
```

**④ 让 AI 能建造雷达站**

在 `DefenseController.gd` 中添加雷达站的建造逻辑。

---

### 案例 3：新增一种 AI 策略（偷袭）

AI 在游戏中期派一小队绕后攻击。

**① 创建新控制器**

```gdscript
# source/match/players/simple-clairvoyant-ai/FlankingController.gd
extends Node

signal resources_required(resources, metadata)

var _player = null
var _has_flanked = false

func setup(player):
    _player = player
    MatchSignals.match_started.connect(_on_match_started)

func _on_match_started():
    # 游戏开始 60 秒后尝试偷袭
    await get_tree().create_timer(60.0).timeout
    if not _has_flanked:
        _try_flank()

func _try_flank():
    # 收集闲置的攻击单位
    var flanking_units = []
    for unit in get_tree().get_nodes_in_group("units"):
        if unit.player == _player and unit is Tank and unit.action == null:
            flanking_units.append(unit)
    if flanking_units.size() >= 3:
        # 绕到敌人后方
        var enemy_base = _find_enemy_base()
        if enemy_base:
            var flank_position = enemy_base.global_transform.origin + Vector3(-5, 0, -5)
            for unit in flanking_units:
                unit.action = preload("res://source/match/units/actions/Moving.gd").new(flank_position)
        _has_flanked = true
```

**② 注册到 AI**

在 `SimpleClairvoyantAI.gd` 中实例化 `FlankingController` 并调用 `setup()`。

---

### 案例 4：新增小地图上的资源显示

**① 在 HUD 层操作**

在 `Minimap.gd` 中添加资源点标记：

```gdscript
# 在 Minimap.gd 中
func _ready():
    MatchSignals.unit_spawned.connect(_on_unit_spawned)

func _on_unit_spawned(unit):
    if unit.is_in_group("resource_units"):
        _draw_resource_icon_on_minimap(unit)

func _draw_resource_icon_on_minimap(unit):
    # 在 minimap 对应位置画一个黄色小点
    pass
```

**② 利用现有信号**

不需要新建信号，`unit_spawned` 已经包含了所有单位生成事件。

---

### 案例 5：新增第三种资源"水晶"

**① 在数据层定义**

```gdscript
# MatchConstants.gd 的 Resources 类中新增
class C:
    const COLOR = Color.PURPLE
    const MATERIAL_PATH = "res://source/match/resources/materials/resource_c.tres"
    const COLLECTING_TIME_S = 3.0
```

**② 创建资源场景**

复制 `ResourceA.tscn` 为 `ResourceC.tscn`，更换模型材质为紫色。

**③ 扩展 Worker**

在 `Worker.gd` 中添加 `resource_c` 属性和 `resources_max_c`。

**④ 扩展采集动作**

在 `CollectingResourcesWhileInRange.gd` 中添加对 ResourceC 的判断。

---

## 五、新增功能时的"检查清单"

每加一个新功能，检查以下问题：

```
□ 文件放对目录了吗？
   └→ 单位放 units/，界面放 hud/，AI放 players/...

□ 注册到 Constants 了吗？
   └→ 属性、成本、时间、蓝图映射

□ 使用了正确的基类？
   └→ 可移动单位 → Unit.gd
   └→ 建筑 → Structure.gd
   └→ 空中单位 → Unit.gd（通过 Movement 的 domain 区分）

□ 组合了正确的 Traits 吗？
   └→ 能移动 → 加 Movement
   └→ 能选中 → 加 Selection
   └→ 能受伤 → 加 HealthBar + Targetability

□ 初始状态设置正确吗？
   └→ 战斗单位 → 初始为 WaitingForTargets
   └→ 建筑 → 初始为构造中

□ 监听了正确的信号吗？
   └→ action_changed → 动作切换时恢复默认状态
   └→ match_started / unit_spawned → 初始化

□ 模型材质中的金色 (0.99, 0.81, 0.48) 被正确替换了吗？
   └→ Unit.gd 会自动把该颜色替换为玩家颜色
```

---

## 六、核心设计模式的"一图流"

```
                 ╔══════════════════════════════╗
                 ║        Match.gd              ║
                 ║     (对局总指挥)              ║
                 ╚══════════════════════════════╝
                            │
          ┌─────────────────┼─────────────────┐
          ▼                 ▼                 ▼
   ╔════════════╗   ╔══════════════╗   ╔══════════════╗
   ║  Player.gd ║   ║ Navigation.gd║   ║ FogOfWar.gd  ║
   ║  (玩家)    ║   ║  (导航)      ║   ║ (战争迷雾)   ║
   ╚════════════╝   ╚══════════════╝   ╚══════════════╝
          │
          ▼  (单位作为子节点)
   ╔══════════════════════════════════════╗
   ║            Unit.gd                   ║
   ║  ┌────────────────────────────────┐  ║
   ║  │  Traits (特性组件):            │  ║
   ║  │  ├─ Movement (移动)           │  ║
   ║  │  ├─ HealthBar (血条)          │  ║
   ║  │  ├─ Selection (选中)          │  ║
   ║  │  └─ Targetability (可被攻击)  │  ║
   ║  ├────────────────────────────────┤  ║
   ║  │  Action (当前动作):           │  ║
   ║  │  └─ Moving / Attacking / ...  │  ║  ← 状态模式
   ║  └────────────────────────────────┘  ║
   ╚══════════════════════════════════════╝
          │
          ▼  (通过信号通信)
   ╔══════════════════════════════════════╗
   ║        MatchSignals                  ║
   ║  unit_selected →  UI更新            ║
   ║  unit_died → AI调整策略             ║
   ║  unit_spawned → 注册到系统          ║
   ╚══════════════════════════════════════╝
```

---

## 七、最后的话

作为初学者，理解这个框架最好的方式不是死记文件结构，而是理解它的**设计思路**：

1. **不要在一处写太多逻辑** → 像这个框架一样，把不同职责拆到不同的文件里
2. **数据放在一起，逻辑放在一起** → 属性去 Constants，行为去 actions/，组件去 traits/
3. **模块之间通过信号通信** → 别让模块互相"认识"，让它们通过事件"喊话"
4. **组合比继承灵活** → 单位不靠继承树分类，靠组件组合能力

当你真正理解并开始按照这个思路去添加功能时，你就已经学会游戏架构设计的基础了！

你已经有了指南文档，配合这份设计理念文档，可以开始动手修改和扩展了。先从简单的入手——比如把坦克的攻击力改大、或者新增一个"火箭兵"单位试试看 🚀
